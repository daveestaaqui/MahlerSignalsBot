If PRO still errors:
  TELEGRAM_BOT_TOKEN=$(grep -E '^TELEGRAM_BOT_TOKEN=' .env.local | cut -d= -f2) \
  TELEGRAM_CHAT_ID_PRO=$(grep -E '^TELEGRAM_CHAT_ID_PRO=' .env.local | cut -d= -f2) \
  ./scripts/tg-debug.sh

Typical Telegram 400 causes:
  • Bot not admin in that chat (fix: add as admin)
  • Chat id incorrect (use the negative numeric id from getUpdates)
  • Sending blocked by chat settings (try a simple "test" text first)
